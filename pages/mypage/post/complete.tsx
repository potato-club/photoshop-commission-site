import type { NextPage } from 'next';
import { MyComplete } from 'src/containers/mypage/post/MyComplete';

const postComplete: NextPage = () => {
  return <MyComplete />;
};

export default postComplete;
